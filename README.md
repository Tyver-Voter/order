# Tyver Order

Create your own ballot preference order guide with a drag and drop interface.

`npm run dev`

`npm run build`

## Adding preset configurations

Create a `configuration.json` file according to the format described in `/custom/configurations/index.html`.

Add the `configuration.json` file to the `/public/` directory. Update `/elections.html`. Re-run `npm run dev` to update the dev server.